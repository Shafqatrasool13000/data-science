import React from 'react';

const ProfileButton = () => {
    return <div>
        <button variant="contained"
            className='save-btn'
        >
            Profile
        </button>
        <button variant="contained"

                className='register-btn'
            >
                Register
            </button>
    </div>;
};

export default ProfileButton;
